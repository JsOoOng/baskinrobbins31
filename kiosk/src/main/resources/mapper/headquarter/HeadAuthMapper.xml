<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kiosk.headquarter.mapper.HeadAuthMapper">

    <select id="findEmployeeByLoginId"
            resultType="com.kiosk.headquarter.dto.auth.HeadLoginEmployeeDTO">
        SELECT
            employee_id AS employeeId,
            store_id AS storeId,
            login_id AS loginId,
            password AS password,
            name AS name,
            role AS role,
            status AS status
        FROM employees
        WHERE login_id = #{loginId}
        LIMIT 1
    </select>

</mapper>