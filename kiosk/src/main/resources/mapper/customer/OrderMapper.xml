<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kiosk.customer.order.repository.OrderMapper">

    <select id="selectOrderById" resultType="com.kiosk.customer.order.dto.OrderResponse">
        SELECT order_id, total_price 
        FROM orders 
        WHERE order_id = #{orderId}
    </select>

   <select id="selectOrderItemsByOrderId" resultType="com.kiosk.customer.order.dto.OrderItemDTO">
    SELECT 
        oi.order_item_id AS id,
        oi.product_id AS productId,
        p.product_name AS productName,
        oi.quantity,
        oi.unit_price AS unitPrice
    FROM order_items oi
    JOIN products p ON oi.product_id = p.product_id
    WHERE oi.order_id = #{orderId}
</select>
    
    <select id="getOrderDetails" resultType="map" parameterType="int">
        SELECT p.product_name, oi.quantity, p.base_price
        FROM order_items oi
        JOIN products p ON oi.product_id = p.product_id
        WHERE oi.order_id = #{orderId}
    </select>

    <update id="decreaseProductStock">
	    UPDATE store_inventory
	    SET current_stock = current_stock - #{quantity}
	    WHERE item_id = #{productId}  AND current_stock >= #{quantity}
	</update>

    <update id="updateOrderStatus">
        UPDATE orders
        SET order_status = #{status}
        WHERE order_id = #{orderId}
    </update>

</mapper>