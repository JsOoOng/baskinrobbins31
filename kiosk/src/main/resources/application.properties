# ===================================================== ====
# 1. Spring Boot Default
# =========================================================
spring.application.name=kiosk
server.port=8889
server.printPort=7777

# ========================================================
# 2. MySQL
# ========================================================
# localhost로 접속


#spring.datasource.url=jdbc:mysql://172.16.15.73:3308/kiosk?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=1234
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=jdbc:mysql://kiosk.c72ggsumojx0.ap-northeast-2.rds.amazonaws.com:3306/kiosk?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=1q2w3e4r!!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================================================
# 3. JPA Hibernate
# =========================================================
#spring.jpa.hibernate.ddl-auto=create 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.default_batch_fetch_size=100

# =========================================================
# 4. MyBatis
# =========================================================
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

# =========================================================
# 5. Etc
# =========================================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
# 맛 이미지 영구 저장 위치. 운영 서버에서는 FLAVOR_UPLOAD_DIR을 영구 볼륨 경로로 지정합니다.
app.upload.flavor-directory=${FLAVOR_UPLOAD_DIR:./data/flavors}
logging.level.com.kiosk=debug
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.leak-detection-threshold=10000

# =========================================================
# 6. Toss Payments API
# =========================================================
toss.secret-key=test_sk_PBal2vxj811glNODl9dk85RQgOAN
spring.jpa.defer-datasource-initialization=true

#spring.sql.init.mode=always
spring.sql.init.mode=never

spring.sql.init.data-locations=classpath:init_data.sql
spring.sql.init.encoding=UTF-8
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4

# =========================================================
# 7. Cloudflare Turnstile
# =========================================================
cloudflare.turnstile.secret-key=0x4AAAAAAEF4jKbu4JipMgJ4wtm6z3NUT-k
