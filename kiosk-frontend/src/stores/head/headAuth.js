/**
 * [모듈 흐름 안내] headAuth
 * 역할: 본사 인증 API를 배치하기 위해 준비한 모듈이며 현재 실행되는 요청 코드는 없다.
 * 호출 흐름: 향후 Vue 화면 -> 이 모듈의 API 함수 -> Axios -> Spring Controller 순서로 연결할 자리다.
 * 데이터 기준: 실제 요청 URL과 현재 백엔드 DTO 필드를 우선한다.
 */
